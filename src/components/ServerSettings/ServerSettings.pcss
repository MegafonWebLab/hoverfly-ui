$b: .server-settings;

$(b) {
    &__header {
        margin-bottom: 12px;

        font-weight: 600;

        font-size: 32px;
        line-height: 40px;
    }

    &__global-delay-input {
        max-width: 265px;

        border-radius: 12px;
    }
}
